{
  "name": "claude-verbs-cli",
  "version": "0.1.0",
  "description": "Themed spinner verb sets for Claude Code",
  "author": "JJ",
  "license": "MIT",
  "type": "module",
  "bin": {
    "claude-verbs": "src/cli.ts"
  },
  "files": ["src", "sets"],
  "keywords": ["claude", "claude-code", "spinner", "cli", "verbs"],
  "scripts": {
    "cli": "bun src/cli.ts",
    "build": "tsc",
    "prepublishOnly": "bun run build && bun test && bun run lint",
    "test": "vitest run",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
